var a;

proc p
    var b, c, d;

    a := a + 1;
    a := a - 1;
    a := a * 1;
    a := (a);

    a := a == 1;
    a := a < 1;
    a := a > 1;
    a := !a;
    a := a && a;
    a := a || a;

    a := (a + 1) * b - (c + d) * (a == b) < 1 && !a || (a + (b + c) * d) == 1

end
